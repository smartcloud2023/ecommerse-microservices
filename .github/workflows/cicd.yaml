name: Build All Service Image

on:
  push:
    branches:
      - main
     
    

jobs:
  build-cart:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

# AWS Credentials 
#================
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

# Login to ECR
#=============
      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2


      # Create repo if not exists
      #==========================
      - name: Create ECR repository if needed
        run: |
            repos=(
            accounting ad cart checkout currency email flagd flagd-ui fraud-detection
            frontend frontend-proxy grafana image-provider jaeger kafka llm
            load-generator opensearch otel-collector payment postgresql
            product-catalog product-reviews prometheus quote react-native-app
            recommendation shipping
            )

            for repo in "${repos[@]}"; do
            aws ecr describe-repositories --repository-names "$repo" >/dev/null 2>&1 \
            || aws ecr create-repository --repository-name "$repo"
            done


        # # Build and push Docker images
        # #============================
        # - name: Build and push Docker images
        #   run: |
        #     for service in accounting ad cart checkout currency email flagd flagd-ui fraud-detection frontend frontend-proxy grafana image-provider jaeger kafka llm load-generator opensearch otel-collector payment postgresql product-catalog product-reviews prometheus quote react-native-app recommendation shipping; do
        #       echo "Building and pushing $service..."
        #       docker build -t $service:latest ./src/$service
        #       docker tag $service:latest ${{ steps.login-ecr.outputs.registry }}/${{ steps.login-ecr.outputs.repository }}:$service-latest
        #       docker push ${{ steps.login-ecr.outputs.registry }}/${{ steps.login-ecr.outputs.repository }}:$service-latest
        #     done